首页重建 v3 说明

本版针对 v2 中“追光和视差不可见”的问题进行修正：
1. 移除首页 reduced-motion 对视差的阻断；
2. 增大但仍保持稳定的山/窗/仕女分层位移幅度；
3. 增强局部追光，但保留小范围光斑，不做全屏泛黄；
4. JS 仍只更新 CSS 变量，不直接写图层 transform；
5. 不使用 transform: !important，不启用旧 heroCanvas，不裁切窗框素材。

只修改：assets/css/styles.css、assets/js/app.js。
